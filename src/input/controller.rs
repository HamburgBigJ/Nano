use bevy::app::Plugin;

pub struct GameController;

impl Plugin for GameController {
    fn build(&self, app: &mut bevy::prelude::App) {
        
    }
}

// contller like mose click to summon sand and richt click to remvoe
// ui is missing for teh dirfent kinds but shoud be done in IMGUI